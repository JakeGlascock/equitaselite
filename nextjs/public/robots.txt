# Equitas Elite is invitation-only and not yet ready for indexing.
# The global meta `noindex` tag in src/app/layout.tsx already blocks
# well-behaved crawlers from indexing rendered pages; this file blocks
# crawl entirely as a belt-and-suspenders measure.
#
# When ready to launch publicly: remove this file AND the
# `robots: { index: false, follow: false }` metadata in app/layout.tsx.

User-agent: *
Disallow: /
